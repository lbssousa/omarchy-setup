# Camada estática para IDEs e todos os processos da sessão systemd.
# Lido pelo systemd --user na inicialização; herdado por Hyprland,
# terminais, VSCode, Zed, etc.
SSH_AUTH_SOCK=$HOME/.bitwarden-ssh-agent.sock
