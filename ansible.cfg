[defaults]
inventory = inventory.ini
host_key_checking = False
stdout_callback = default
display_skipped_hosts = False

[privilege_escalation]
# Default sudo become, used with --ask-become-pass (see Justfile). The
# fingerprint reader is tried first via PAM on every privileged task; the
# password is the fallback if it doesn't cooperate.
